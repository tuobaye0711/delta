# 部署配置检查清单

> ✅ 部署前需要准备的所有事项

---

## 选择部署方案

在开始部署前，先选择一个方案：

- [ ] **方案 A**：只部署前端到 Vercel（推荐新手）
  - 前端：Vercel（免费）
  - 后端：Render（免费）
  - 数据库：Render PostgreSQL（90天免费）

- [ ] **方案 B**：全栈部署到 Vercel（推荐进阶）
  - 前端+后端：Vercel（免费）
  - 数据库：Supabase（免费）

- [ ] **方案 C**：全部部署到 Render
  - 前端+后端+数据库：Render（免费）

---

## 通用准备（所有方案都需要）

### 1. Git 仓库

- [ ] 创建 GitHub/GitLab/Bitbucket 仓库
- [ ] 将项目代码推送到仓库

```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/your-username/delta-force-tracker.git
git push -u origin main
```

### 2. 账号注册

- [ ] Vercel 账号：https://vercel.com
- [ ] Render 账号：https://render.com
- [ ] Supabase 账号（如使用方案B）：https://supabase.com

---

## 方案 A：前端到 Vercel + 后端到 Render

### 前端准备（Vercel）

- [ ] 确认 `frontend/package.json` 存在
- [ ] 确认 `vite.config.ts` 配置正确
- [ ] 测试本地构建：`cd frontend && npm run build`

### 后端准备（Render）

- [ ] 修改 `backend/prisma/schema.prisma`：
  ```prisma
  datasource db {
    provider = "postgresql"
    url      = env("DATABASE_URL")
  }
  ```
- [ ] 安装 PostgreSQL 客户端：`cd backend && npm install pg`
- [ ] 修改 `backend/src/index.ts` 添加 Graceful Shutdown
- [ ] 确认 `backend/package.json` 有正确脚本：
  ```json
  {
    "scripts": {
      "start": "node dist/index.js",
      "build": "tsc",
      "postinstall": "npx prisma generate"
    }
  }
  ```
- [ ] 测试本地构建：`cd backend && npm run build`

### 数据库准备（Render PostgreSQL）

- [ ] 在 Render 创建 PostgreSQL 数据库
- [ ] 复制数据库连接字符串
- [ ] 记录数据库信息：
  - Host: _________________
  - Port: _________________ (5432)
  - Database: _________________
  - User: _________________
  - Password: _________________
  - Connection URL: _________________

---

## 方案 B：全栈部署到 Vercel

### 前端准备

- [ ] 确认 `frontend/package.json` 存在
- [ ] 确认 `vite.config.ts` 配置正确
- [ ] 测试本地构建：`cd frontend && npm run build`

### 后端准备

- [ ] 修改 `backend/prisma/schema.prisma`：
  ```prisma
  datasource db {
    provider = "postgresql"
    url      = env("DATABASE_URL")
  }
  ```
- [ ] 安装 PostgreSQL 客户端：`cd backend && npm install pg`
- [ ] 创建 `vercel.json` 配置文件
- [ ] 测试本地构建：`cd backend && npm run build`

### 数据库准备（Supabase）

- [ ] 在 Supabase 创建项目
- [ ] 复制数据库连接字符串
- [ ] 记录数据库信息：
  - Host: _________________
  - Port: _________________ (5432)
  - Database: _________________ (postgres)
  - User: _________________ (postgres)
  - Password: _________________
  - Connection URL: _________________

---

## 方案 C：全部部署到 Render

### 前端准备

- [ ] 确认 `frontend/package.json` 存在
- [ ] 确认 `vite.config.ts` 配置正确
- [ ] 创建 Render 静态站点配置

### 后端准备

- [ ] 修改数据库配置为 PostgreSQL
- [ ] 安装 PostgreSQL 客户端
- [ ] 修改后端代码以适配 Render
- [ ] 测试本地构建

### 数据库准备

- [ ] 在 Render 创建 PostgreSQL 数据库
- [ ] 复制数据库连接字符串

---

## 环境变量清单

### Vercel 需要的环境变量

| 变量名 | 说明 | 方案 A | 方案 B | 方案 C |
|-------|------|--------|--------|--------|
| DATABASE_URL | 数据库连接字符串 | - | ✅ | - |
| VITE_API_URL | 后端 API 地址 | ✅ | - | ✅ |

### Render 需要的环境变量

| 变量名 | 说明 | 方案 A | 方案 B | 方案 C |
|-------|------|--------|--------|--------|
| DATABASE_URL | 数据库连接字符串 | ✅ | - | ✅ |
| NODE_ENV | 运行环境 | ✅ | - | ✅ |
| PORT | 端口号 | ✅ | - | ✅ |

---

## 部署后检查清单

### 前端部署后

- [ ] 访问前端 URL 确认页面加载正常
- [ ] 检查浏览器控制台是否有错误
- [ ] 确认 API 请求能正常发送

### 后端部署后

- [ ] 访问 `/health` 端点确认服务正常
- [ ] 访问 `/api/bullets` 确认 API 可用
- [ ] 检查 Render 日志确认没有错误

### 数据库初始化后

- [ ] 运行数据库迁移
- [ ] 初始化弹种数据
- [ ] 测试添加交易记录
- [ ] 测试价格查询

---

## 域名配置（可选）

- [ ] 购买域名（如 Namecheap、GoDaddy、阿里云）
- [ ] 在 Vercel 添加自定义域名
- [ ] 在域名注册商配置 DNS
- [ ] 等待 DNS 生效（通常 5-60 分钟）

---

## 监控和维护

- [ ] 配置错误监控（如 Sentry）
- [ ] 设置日志告警
- [ ] 配置数据库备份
- [ ] 定期检查服务状态

---

## 备份策略

- [ ] 数据库自动备份（Render 提供）
- [ ] 代码 Git 版本控制
- [ ] 定期导出配置文件

---

## 需要我帮忙什么？

请告诉我你选择的方案，我会帮你：

### 如果选择方案 A
- [ ] 修改前端 API 配置
- [ ] 准备 Render 部署配置
- [ ] 提供部署步骤指导

### 如果选择方案 B
- [ ] 修改数据库配置
- [ ] 创建 Vercel 配置文件
- [ ] 提供数据库迁移指导

### 如果选择方案 C
- [ ] 准备全 Render 部署配置
- [ ] 提供前后端配置指导

---

## 快速开始命令

### 准备推送代码

```bash
# 初始化 Git
git init

# 添加所有文件
git add .

# 提交
git commit -m "Initial commit"

# 添加远程仓库
git remote add origin https://github.com/your-username/delta-force-tracker.git

# 推送到 GitHub
git push -u origin main
```

### 测试本地构建

```bash
# 测试前端
cd frontend
npm run build

# 测试后端
cd ../backend
npm run build
```

---

## 文件检查

部署前确认以下文件存在：

- [ ] `frontend/package.json`
- [ ] `frontend/vite.config.ts`
- [ ] `frontend/src/main.ts`
- [ ] `backend/package.json`
- [ ] `backend/prisma/schema.prisma`
- [ ] `backend/src/index.ts`
- [ ] `backend/tsconfig.json`
- [ ] `.gitignore`

---

## 需要的信息

请准备好以下信息：

### Git 仓库
- 仓库 URL: _________________
- 分支名称: _________________ (main)

### 数据库
- Host: _________________
- Port: _________________
- Database: _________________
- User: _________________
- Password: _________________

### 域名（如果使用）
- 域名: _________________

---

准备好后，告诉我你选择的方案，我会帮你完成部署！🚀

---

Made with 💖 by 盼盼 🐼
